inicra las dependecias dentro de la carpeta api
1. Crear el archivo package.json con valores por defecto
npm init -y

2. Instalar el framework web, el cliente de Redis y AWS SDK
 npm install express redis @aws-sdk/client-s3 dotenv cors

 carpeta sender
 npm init -y
 npm install axios dotenv